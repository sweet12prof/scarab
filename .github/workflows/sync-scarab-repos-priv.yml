name: Sync to Public Repo with Original Authors

permissions:
  contents: write

on:
  push:
    branches:
      - main

jobs:
  sync-repos:
    if: ${{ github.repository == 'litz-lab/scarab_ll' && github.event_name == 'push' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Private Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Ensure we have the full commit history

      - name: Configure Git (Keep Original Authors)
        run: |
          git config --global user.email "$(git log -1 --pretty=format:'%ae')"
          git config --global user.name "$(git log -1 --pretty=format:'%an')"

      - name: Add Public Repository Remote
        run: git remote add public https://${{ secrets.SCARAB_SYNC_PAT }}@github.com/litz-lab/scarab.git

      - name: Push Updates to Public Repository
        run: git push public HEAD:main

